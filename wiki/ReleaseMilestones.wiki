#summary A list of goals to be reached

See the ArchivedReleases page for past release information.
Maybe you're looking for FutureGoals?

= Planned Release Features for v1.0 =

  # Completely open-source engine
  # Script loading w/ Dependency processor (linker)
  # Multiple browser support
    * Firefox 1.5+
    * Safari 3.0+
    * Opera 9.5+
    * Chrome b3+
  # 2D Vector-based example game (Asteroids clone)
  # 2D DOM-based example game (Poker/Tetris mashup)
  # DOM rendering context
  # Canvas rendering context
  # Modular host objects
  # Multiple components
    * Transformation
    * Collision
    * Input (keyboard, mouse, Wii remote)
    * Logic 
    * Rendering (Canvas, DOM)
  # 2D Sprite support
  # Sound support
  # Resource loaders
    * JSON Object
    * XML
    * Sound
    * Image
    * Sprite (animated & static)
    * Bitmapped font
  # Ant build script
  # 2D Sprite editor
  # 2D Font editor
  # 2D Particle effect editor
  # Multiple platforms
    * Windows
    * Macintosh
    * Linux
    * iPhone
    * Wii

= Release Goals and Milestones =

The following list is a set of goals to be reached for the intended release.  Some goals may be added to finalize a release, others may be removed to advance a release's timeline.

== Beta 1.2 (January 9, 2009) ==

|| *Status* || *Goal* || *Description* ||
|| _Complete_ || Tool || Ant build script. ||
|| _Complete_ || Demo || Vector demo - Asteroids clone.  Test for game logic, component loading, dependency checking, vector rendering to 2D context. ||
|| _Complete_ || Browser Support || Opera for Wii - It's sloooow ||
|| _80%_ || Tool || Update Ant build script to include only complete tools and demos. ||

== Beta 1.3 (Tentatively January 30, 2009) ==

|| *Status* || *Goal* || *Description* ||
|| _10%_ || Dev || Better handling of DOM as a viable render context.  Include demo game of Poker/Tetris mashup. ||

== Beta 1.4 (TBA) ==
|| *Status* || *Goal* || *Description* ||
|| _60%_ || Tool || Sprite editor.  A tool for making, saving and loading, and animating 2D sprites. ||

== Beta 1.5 (TBA) ==

|| *Status* || *Goal* || *Description* ||
|| _5%_ || Tool || Bitmap font editor for browsers which don't support fonts in canvas context. ||
|| - || Tool || Particle editor. ||

== Beta 2 (TBA) ==

|| *Status* || *Goal* || *Description* ||
|| - || *Code freeze* || *No additional features* - Start logging and addressing major bugs. ||

== Release Candidate 1 (TBA) ==

|| *Status* || *Goal* || *Description* ||
|| - || Bug Fixing || Fix bugs from beta 2 ||
 

== Release Candidate 2 (TBA) ==

|| *Status* || *Goal* || *Description* ||
|| - || Bug Fixing || Fix bugs found during RC1. ||


== Gold Candidate - Version 1.0 (TBA) ==

|| *Status* || *Goal* || *Description* ||
|| - || Bug Fixing || Fix bugs found during RC2. ||
|| - || Final || This release will be tagged v1.0 ||